<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>AI Model News</title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600&display=swap" rel="stylesheet">
  <style>
    body { font-family: Inter, system-ui, -apple-system, sans-serif; margin: 0; color: #111; background: #f7f7f7; }
    header { background: #111; color: #fff; padding: 24px; }
    header h1 { margin: 0; font-size: 22px; }
    header p { margin: 8px 0 0; color: #ccc; }
    main { max-width: 800px; margin: 24px auto; background: #fff; padding: 24px; border-radius: 12px; box-shadow: 0 6px 24px rgba(0,0,0,0.06); }
    .date { color: #666; font-size: 14px; margin-bottom: 16px; }
    .item { border-bottom: 1px solid #eee; padding: 16px 0; }
    .item:last-child { border-bottom: 0; }
    .item a { color: #0b69ff; text-decoration: none; }
    .item a:hover { text-decoration: underline; }
    .tags { font-size: 12px; color: #555; margin-top: 8px; }
    .empty { text-align: center; color: #555; padding: 40px 0; }
    footer { text-align: center; color: #666; padding: 16px; font-size: 13px; }
  </style>
</head>
<body>
  <header>
    <h1>AI Model News</h1>
    <p>Daily highlights in detection, classification, segmentation, and LLMs.</p>
  </header>
  <main>
    <div class="date">Updated {{ generated_date }}</div>
    {% if notable and notable|length > 0 %}
      <h2>Notable Releases</h2>
      {% for n in notable %}
      <div class="item">
        <a href="{{ n.link }}" target="_blank" rel="noopener">{{ n.title }}</a>
        <div class="tags">From {{ n.source }} • {{ n.published | replace('T',' ') | replace('+00:00',' UTC') }} • Categories: {{ n.categories | join(', ') }}</div>
        {% if n.summary %}
        <div class="summary">{{ n.summary }}</div>
        {% endif %}
      </div>
      {% endfor %}
      <hr />
    {% endif %}
    {% if items and items|length > 0 %}
      {% for item in items %}
      <div class="item">
        <a href="{{ item.link }}" target="_blank" rel="noopener">{{ item.title }}</a>
        <div class="tags">From {{ item.source }} • {{ item.published | replace('T',' ') | replace('+00:00',' UTC') }} • Categories: {{ item.categories | join(', ') }}</div>
        {% if item.summary %}
        <div class="summary">{{ item.summary }}</div>
        {% endif %}
      </div>
      {% endfor %}
    {% else %}
      <div class="empty">No updates today. Check back tomorrow at 8:00 AM ET.</div>
    {% endif %}
  </main>
  <footer>
    Built and published automatically via GitHub Pages.
  </footer>
</body>
</html>
